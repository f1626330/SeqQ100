<UserControl x:Class="Sequlite.ALF.FirmwareUpgrader.MainboardUpgradeOptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sequlite.ALF.FirmwareUpgrader"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!--  Upgrading target selection  -->
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="Select Upgrading Target:" Margin="5"/>
            <Button Content="Mainboard" Margin="5" Command="{Binding Path=SetCmd}" CommandParameter="TargetMB"/>
            <Button Content="Motion Controller upgrade loader" Margin="5" Command="{Binding Path=SetCmd}" CommandParameter="TargetMotionLoader"/>
            <Button Content="Motion Controller Firmware" Margin="5" Command="{Binding Path=SetCmd}" CommandParameter="TargetMotionFw"/>
        </StackPanel>
    </Grid>
</UserControl>
