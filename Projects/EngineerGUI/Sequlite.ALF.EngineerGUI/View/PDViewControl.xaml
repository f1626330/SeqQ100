<UserControl x:Class="Sequlite.ALF.EngineerGUI.View.PDViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sequlite.ALF.EngineerGUI.View"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="PD Value" Margin="5" VerticalAlignment="Center"/>
                <TextBox Width="60" Margin="5,5,0,5" VerticalAlignment="Center"
                         Text="{Binding Path=PDValue}"/>
                <Button Content="Read" Width="40" Margin="5" VerticalAlignment="Center" Command="{Binding Path=GetPDValueCmd}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
